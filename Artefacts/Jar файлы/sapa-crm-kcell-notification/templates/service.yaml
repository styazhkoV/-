apiVersion: v1
kind: Service
metadata:
  name: {{ include "sapa-crm-kcell-notification.fullname" . }}
  labels:
    {{- include "sapa-crm-kcell-notification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sapa-crm-kcell-notification.selectorLabels" . | nindent 4 }}
