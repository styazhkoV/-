apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sapa-crm-kcell-client.fullname" . }}-cm
data:
  {{- $files := .Files }}
  {{- range tuple "vars.txt" }}
  {{ . }}: |-
    {{ $files.Get . }}
  {{- end }}
