apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sapa-crm-kcell-task.fullname" . }}-test-connection"
  labels:
    {{- include "sapa-crm-kcell-task.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sapa-crm-kcell-task.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
