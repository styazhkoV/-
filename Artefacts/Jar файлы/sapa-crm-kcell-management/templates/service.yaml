apiVersion: v1
kind: Service
metadata:
  name: {{ include "sapa-crm-kcell-management.fullname" . }}
  labels:
    {{- include "sapa-crm-kcell-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sapa-crm-kcell-management.selectorLabels" . | nindent 4 }}
