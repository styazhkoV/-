apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sapa-crm-kcell-task.fullname" . }}-cm
data:
  {{- $files := .Files }}
  {{- range tuple "vars.txt" }}
  {{ . }}: |-
    {{ $files.Get . }}
  {{- end }}

